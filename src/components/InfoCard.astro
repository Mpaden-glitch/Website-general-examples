---
interface Props {
  title: string;
  text: string;
  ctaLabel?: string;
  href?: string;
}

const { title, text, ctaLabel = 'Learn More', href = '#' } = Astro.props;
---

<article class="info-card">
  <h3>{title}</h3>
  <p>{text}</p>
  <a href={href}>{ctaLabel}</a>
</article>
